##############################################
# 🧪 FUNKARD — AMBIENTE DI SVILUPPO (LOCALE)
##############################################

# === 🔌 Database Neon (stesso di produzione, ma con log attivi)
spring.datasource.url=jdbc:postgresql://ep-old-morning-agjdnypv-pooler.c-2.eu-central-1.aws.neon.tech/neondb
spring.datasource.username=neondb_owner
spring.datasource.password=npg_b7l2KWDfEeTP

# === 🧠 JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# === ⚙️ Server locale
server.port=8080
server.error.include-message=always
server.error.include-binding-errors=always

# === 🌍 CORS aperto (per test da localhost o altri domini)
funkard.frontend.url=*

# === 🔐 JWT
jwt.secret=FUNKARD_DEV_KEY_2025
jwt.expiration=86400000

# === 📜 Logging esteso
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.zaxxer.hikari=INFO
logging.level.com.funkard=DEBUG

# === 🏷️ Metadata
spring.application.name=Funkard API (DEV)

# === 📧 Email Configuration (DEV)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# === 🔔 Admin notifications
admin.email=${ADMIN_EMAIL:}

# === 🔐 Admin authentication
admin.token=${ADMIN_TOKEN:FUNKARD_ADMIN_DEV_TOKEN_2025}
