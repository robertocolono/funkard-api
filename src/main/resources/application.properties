
##############################################
# 🌐 FUNKARD — BACKEND API (PRODUCTION)
##############################################

# === 🔌 Database (Neon PostgreSQL)
spring.datasource.url=jdbc:postgresql://ep-old-morning-agjdnypv-pooler.c-2.eu-central-1.aws.neon.tech/neondb
spring.datasource.username=neondb_owner
spring.datasource.password=npg_Yqd62mnGCFNb

# === 🧠 JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# === 🌱 Connection Pool (Hikari)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=600000

# === ⚙️ Server Config
server.port=8080
server.error.include-message=always
server.error.include-binding-errors=always

# === 🌍 CORS per dominio Vercel
# Permette richieste solo dal frontend ufficiale
funkard.frontend.url=https://funkard.vercel.app

# === 🔐 JWT Config (usato da JwtUtil)
jwt.secret=FUNKARD_SUPER_SECRET_KEY_2025
jwt.expiration=86400000

# === 📜 Logging
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=ERROR
logging.level.com.funkard=INFO
logging.file.name=logs/funkard-api.log

# === 🏷️ App metadata
spring.application.name=Funkard Api
spring.profiles.active=prod

# === 🔍 GradeLens - gratuito
AI_PROVIDER=none
